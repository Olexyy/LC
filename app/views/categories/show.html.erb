<%= bootstrap_row %>
  <%= bootstrap_col_12 %>
    <%= h1 t(:category) %>
  <%= bootstrap_close %>
<%= bootstrap_close %>
<%= bootstrap_row %>
  <%= bootstrap_col_6 HtmlHelper::CSS_SHOW %>
    <%= p (labl t(:name)) + ': ' + @category.name %>
  <%= bootstrap_close %>
<%= bootstrap_close %>
<%= bootstrap_row %>
  <%= bootstrap_col_6 HtmlHelper::CSS_SHOW %>
    <%= p (labl t(:weight)) + ': ' + @category.weight.to_s %>
  <%= bootstrap_close %>
<%= bootstrap_close %>
<%= bootstrap_lower_menu 6 %>
  <%= link_to t(:edit), edit_category_path(@category) %>
  <%= link_to t(:delete), category_path(@category), method: :delete, data: { confirm: 'Are you sure?' }  %>
  <%= link_to t(:back), categories_path %>
<%= bootstrap_lower_menu_close %>