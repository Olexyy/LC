CKEDITOR.editorConfig = function (config) {
    // ... other configuration ...
    config.language = 'uk';

    config.removeButtons = 'Image,Table,HorizontalRule,SpecialChar,About,Link,Unlink,Anchor';

    // ... rest of the original config.js  ...
}
